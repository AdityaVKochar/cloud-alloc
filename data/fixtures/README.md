# Synthetic fixture

Run `python -m flask demo seed` to generate `synthetic_workloads.parquet` and its checksum manifest in this directory. The generator is deterministic and intentionally small. Its results validate software behavior only and must not be used as research evidence.
